Flask==3.0.0
requests==2.31.0
Pillow==10.1.0
opencv-python-headless==4.8.1.78
numpy==1.26.2
scikit-image==0.22.0
ImageHash==4.3.1
psutil==5.9.6

# GPU acceleration with PyTorch
# NOTE: PyTorch with CUDA is installed automatically in the Docker container
# If running locally, install with:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# torch>=2.0.0
# torchvision>=0.15.0
